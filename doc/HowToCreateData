rake db:migrate
script/create_data.rb

run 'script/console'
--require 'script/logreader.rb'
--ctrl-C then ctrl-D when script is finished

this part is done in the migration
#run 'script/console' again
#--v = ViewPreference.new()
#--v.defaults()
#--v.project_id = 1050
#--v.save

script/server
browse to: localhost:3000/project
